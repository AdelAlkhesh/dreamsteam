<h1>Edit list</h1>
<h1>{{result.name}}</h1>
<form method="POST" action="/lists/{{result._id}}/edit">
    <div class="mb-3">
        <label class="form-label">Title</label>
        <input class="form-control" value="{{result.name}}" type="text" name="name">
        <br>
        <label class="form-label">Description</label>
        <input class="form-control" value="{{result.description}}" type="text" name="description">
        <br>
        <!-- <form action="/games" method="GET">
            {{#if query}}
            <input type="text" placeholder="Search Game" name="search" value="{{query}}" />
            {{else}}
            <input type="text" placeholder="Search Game" name="search" />
            {{/if}}
        
            <button type="submit">Search</button>
        </form> -->
        <br>
        <button class="btn btn-primary" type="submit">EDIT</button>
    </div>
</form>